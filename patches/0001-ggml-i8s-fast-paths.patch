diff --git a/ggml/src/ggml-aarch64.c b/ggml/src/ggml-aarch64.c
index 99f43c6..f03d62d 100644
--- a/ggml/src/ggml-aarch64.c
+++ b/ggml/src/ggml-aarch64.c
@@ -1223,50 +1223,11 @@ void ggml_gemm_i8_i8(int n, int32_t * GGML_RESTRICT s, size_t bs, const void * G
     }
 #endif
 
-#if defined(VAE_ACT_PARALLEL)
-    const int64_t row_block = VAE_ROW_BLOCK_SIZE;
-    const int64_t col_block = VAE_COL_BLOCK_SIZE;
-
-    for (int64_t c0 = 0; c0 < nc; c0 += col_block) {
-        int64_t cur_c = (c0 + col_block <= nc) ? col_block : (nc - c0);
-        for (int64_t r0 = 0; r0 < nr; r0 += row_block) {
-            int64_t cur_r = (r0 + row_block <= nr) ? row_block : (nr - r0);
-            const void * vy_r = (const int8_t *)vy + r0 * n;
-            for (int64_t c = 0; c < cur_c; ++c) {
-                const int64_t col = c0 + c;
-                int32_t * s_col = s + col;
-                const void * vx_col = (const int8_t *)vx + col * n;
-                if (cur_r % VAE_PARALLEL_SIZE == 0) {
-                    ggml_vec_dot_i8_i8(n, s_col + r0 * bs, bs, vx_col, n, vy_r, n, cur_r);
-                } else {
-                    // nrc=1 per call avoids 1x1 path misinterpreting vx as multi-row
-                    for (int64_t r = 0; r < cur_r; ++r) {
-                        const int8_t * vy_row = (const int8_t *)vy_r + r * n;
-                        ggml_vec_dot_i8_i8(n, s_col + (r0 + r) * bs, 0,
-                            vx_col, 0, vy_row, 0, 1);
-                    }
-                }
-            }
-        }
-    }
-#else
-    const int64_t row_block = VAE_ROW_BLOCK_SIZE;
-    const int64_t col_block = VAE_COL_BLOCK_SIZE;
-
-    for (int64_t r0 = 0; r0 < nr; r0 += row_block) {
-        int64_t cur_r = (r0 + row_block <= nr) ? row_block : (nr - r0);
-        for (int64_t c0 = 0; c0 < nc; c0 += col_block) {
-            int64_t cur_c = (c0 + col_block <= nc) ? col_block : (nc - c0);
-            const void * vx_c = (const int8_t *)vx + c0 * n;
-            for (int64_t r = 0; r < cur_r; ++r) {
-                const int64_t row = r0 + r;
-                int32_t * s_row = s + row * bs;
-                const void * vy_row = (const int8_t *)vy + row * n;
-                ggml_vec_dot_i8_i8(n, s_row + c0, bs, vx_c, n, vy_row, n, cur_c);
-            }
-        }
-    }
-#endif
+    // Register-tiled INT8 GEMM (src/ggml-vae-i8_s-mad.cpp). Replaces the
+    // vec_dot-per-(row-block, column) loop that used to live here, which
+    // re-read one operand for every step of the other. Falls back to that
+    // loop internally without AVX512-VNNI, so behaviour is unchanged there.
+    ggml_gemm_i8_i8_tiled(n, s, bs, vx, vy, nr, nc);
 }
 
 void ggml_gemm_q4_0_4x4_q8_0(int n, float * restrict s, size_t bs, const void * restrict vx, const void * restrict vy, int nr, int nc) {
diff --git a/ggml/src/ggml.c b/ggml/src/ggml.c
index 23303be..192ff84 100644
--- a/ggml/src/ggml.c
+++ b/ggml/src/ggml.c
@@ -18726,108 +18726,14 @@ static void ggml_compute_forward_add_scaled(
     float local_absmax = 0.0f;
 
 #if defined(__AVX2__)
-    if (gamma_ne0 != 1) {
-        const __m256 v_inv_a = _mm256_set1_ps(1.0f / a_scale);
-        const __m256 v_inv_b = _mm256_set1_ps(1.0f / b_scale);
-        const __m256 v_sign_mask = _mm256_set1_ps(-0.0f);
-        __m256 vmax = _mm256_setzero_ps();
-
-        int64_t i = i0;
-        // align to ne0 boundary for correct gamma indexing
-        int64_t row_offset = i0 % ne0;
-        if (row_offset != 0) {
-            int64_t row_end = i0 + (ne0 - row_offset);
-            if (row_end > i1) row_end = i1;
-            for (; i < row_end; i++) {
-                float gamma = gamma_data[i % ne0];
-                float val = (float)a_data[i] / a_scale * gamma + (float)b_data[i] / b_scale;
-                float_buf[i] = val;
-                float av = val < 0.0f ? -val : val;
-                if (av > local_absmax) local_absmax = av;
-            }
-        }
-        // main loop: process full rows in chunks of 8
-        for (; i + 7 < i1; i += 8) {
-            int64_t gi = i % ne0;
-            // if we cross a row boundary within this chunk, fall back to scalar
-            if (gi + 8 > ne0) {
-                for (int64_t j = 0; j < 8; j++) {
-                    float gamma = gamma_data[(i + j) % ne0];
-                    float val = (float)a_data[i + j] / a_scale * gamma + (float)b_data[i + j] / b_scale;
-                    float_buf[i + j] = val;
-                    float av = val < 0.0f ? -val : val;
-                    if (av > local_absmax) local_absmax = av;
-                }
-                continue;
-            }
-            __m128i ai8 = _mm_loadl_epi64((const __m128i *)(a_data + i));
-            __m128i bi8 = _mm_loadl_epi64((const __m128i *)(b_data + i));
-            __m256i ai32 = _mm256_cvtepi8_epi32(ai8);
-            __m256i bi32 = _mm256_cvtepi8_epi32(bi8);
-            __m256 af = _mm256_cvtepi32_ps(ai32);
-            __m256 bf = _mm256_cvtepi32_ps(bi32);
-            __m256 gf = _mm256_loadu_ps(gamma_data + gi);
-            __m256 val = _mm256_add_ps(
-                _mm256_mul_ps(_mm256_mul_ps(af, v_inv_a), gf),
-                _mm256_mul_ps(bf, v_inv_b));
-            _mm256_storeu_ps(float_buf + i, val);
-            __m256 av = _mm256_andnot_ps(v_sign_mask, val);
-            vmax = _mm256_max_ps(vmax, av);
-        }
-        // scalar tail
-        for (; i < i1; i++) {
-            float gamma = gamma_data[i % ne0];
-            float val = (float)a_data[i] / a_scale * gamma + (float)b_data[i] / b_scale;
-            float_buf[i] = val;
-            float av = val < 0.0f ? -val : val;
-            if (av > local_absmax) local_absmax = av;
-        }
-        // horizontal max
-        __m128 hi = _mm256_extractf128_ps(vmax, 1);
-        __m128 lo = _mm256_castps256_ps128(vmax);
-        __m128 m = _mm_max_ps(hi, lo);
-        m = _mm_max_ps(m, _mm_movehl_ps(m, m));
-        m = _mm_max_ps(m, _mm_shuffle_ps(m, m, 1));
-        float simd_max = _mm_cvtss_f32(m);
-        if (simd_max > local_absmax) local_absmax = simd_max;
-    } else {
-        // scalar gamma (gamma_ne0 == 1)
-        const float gamma_scalar = gamma_data[0];
-        const __m256 v_inv_a = _mm256_set1_ps(1.0f / a_scale);
-        const __m256 v_inv_b = _mm256_set1_ps(1.0f / b_scale);
-        const __m256 v_gamma = _mm256_set1_ps(gamma_scalar);
-        const __m256 v_sign_mask = _mm256_set1_ps(-0.0f);
-        __m256 vmax = _mm256_setzero_ps();
-
-        int64_t i = i0;
-        for (; i + 7 < i1; i += 8) {
-            __m128i ai8 = _mm_loadl_epi64((const __m128i *)(a_data + i));
-            __m128i bi8 = _mm_loadl_epi64((const __m128i *)(b_data + i));
-            __m256i ai32 = _mm256_cvtepi8_epi32(ai8);
-            __m256i bi32 = _mm256_cvtepi8_epi32(bi8);
-            __m256 af = _mm256_cvtepi32_ps(ai32);
-            __m256 bf = _mm256_cvtepi32_ps(bi32);
-            __m256 val = _mm256_add_ps(
-                _mm256_mul_ps(_mm256_mul_ps(af, v_inv_a), v_gamma),
-                _mm256_mul_ps(bf, v_inv_b));
-            _mm256_storeu_ps(float_buf + i, val);
-            __m256 av = _mm256_andnot_ps(v_sign_mask, val);
-            vmax = _mm256_max_ps(vmax, av);
-        }
-        for (; i < i1; i++) {
-            float val = (float)a_data[i] / a_scale * gamma_scalar + (float)b_data[i] / b_scale;
-            float_buf[i] = val;
-            float av = val < 0.0f ? -val : val;
-            if (av > local_absmax) local_absmax = av;
-        }
-        __m128 hi = _mm256_extractf128_ps(vmax, 1);
-        __m128 lo = _mm256_castps256_ps128(vmax);
-        __m128 m = _mm_max_ps(hi, lo);
-        m = _mm_max_ps(m, _mm_movehl_ps(m, m));
-        m = _mm_max_ps(m, _mm_shuffle_ps(m, m, 1));
-        float simd_max = _mm_cvtss_f32(m);
-        if (simd_max > local_absmax) local_absmax = simd_max;
-    }
+    // Uniform per-element path (src/ggml-vae-i8_s-mad.cpp). The previous code mixed
+    // a reciprocal-multiplying vector loop with divide-flavoured scalar edges at
+    // chunk starts, row crossings and tails -- and those edges move with the thread
+    // partition, so transcripts changed with -t. One code path for every element
+    // makes the op partition-independent.
+    local_absmax = vibeasr_i8s_add_scaled_absmax(
+        a_data + i0, b_data + i0, gamma_data, gamma_ne0, ne0, i0, i1 - i0,
+        a_scale, b_scale, float_buf + i0);
 #elif defined(__ARM_NEON)
     if (gamma_ne0 != 1) {
         const float32x4_t v_inv_a = vdupq_n_f32(1.0f / a_scale);
@@ -18959,29 +18865,10 @@ static void ggml_compute_forward_add_scaled(
     int8_t * dst_data = (int8_t *)dst->data;
 
 #if defined(__AVX2__)
-    {
-        const __m256 v_inv_scale = _mm256_set1_ps(inv_scale);
-        const __m256 v_min = _mm256_set1_ps(-127.0f);
-        const __m256 v_max = _mm256_set1_ps(127.0f);
-        int64_t i = i0;
-        for (; i + 7 < i1; i += 8) {
-            __m256 vf = _mm256_loadu_ps(float_buf + i);
-            vf = _mm256_mul_ps(vf, v_inv_scale);
-            vf = _mm256_max_ps(vf, v_min);
-            vf = _mm256_min_ps(vf, v_max);
-            __m256i vi32 = _mm256_cvtps_epi32(vf);
-            __m256i vi16 = _mm256_packs_epi32(vi32, vi32);
-            vi16 = _mm256_permute4x64_epi64(vi16, 0xD8);
-            __m256i vi8 = _mm256_packs_epi16(vi16, vi16);
-            vi8 = _mm256_permute4x64_epi64(vi8, 0xD8);
-            _mm_storel_epi64((__m128i *)(dst_data + i), _mm256_castsi256_si128(vi8));
-        }
-        for (; i < i1; i++) {
-            float v = float_buf[i] * inv_scale;
-            v = v < -127.0f ? -127.0f : (v > 127.0f ? 127.0f : v);
-            dst_data[i] = (int8_t)roundf(v);
-        }
-    }
+    // Same uniformity argument as step 1; also aligns rounding with the fused ops
+    // (roundf half-away semantics) instead of cvtps' nearest-even.
+    vibeasr_i8s_quant_i8(float_buf + i0, (int8_t *) dst->data + i0, i1 - i0,
+                         inv_scale, /*relu=*/0);
 #elif defined(__ARM_NEON)
     {
         float32x4_t v_inv_scale = vdupq_n_f32(inv_scale);
@@ -19094,12 +18981,11 @@ static void ggml_compute_forward_mul_mat_add(
             // acc_buf layout: [C, n_cols] (channel-major)
             // dst layout: ne[0]=1, ne[1]=N, ne[2]=C => physical [C, N] (channel-major)
             for (int64_t ch = 0; ch < C; ch++) {
-                for (int64_t c = 0; c < n_cols; c++) {
-                    float val = (float)acc_buf[ch * n_cols + c] * combined_scale + bias_data[ch];
-                    float_buf[ch * N_cols + (col0 + c)] = val;
-                    float av = val < 0.0f ? -val : val;
-                    if (av > local_absmax) local_absmax = av;
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                const float amax = vibeasr_i8s_dequant_absmax(
+                    acc_buf + ch * n_cols, n_cols, combined_scale, NULL, bias_data[ch],
+                    float_buf + ch * N_cols + col0);
+                if (amax > local_absmax) local_absmax = amax;
             }
 
         }
@@ -19127,11 +19013,10 @@ static void ggml_compute_forward_mul_mat_add(
 
         if (col0 < col1) {
             for (int64_t ch = 0; ch < C; ch++) {
-                for (int64_t c = col0; c < col1; c++) {
-                    float v = float_buf[ch * N_cols + c] * inv_scale;
-                    v = v < -127.0f ? -127.0f : (v > 127.0f ? 127.0f : v);
-                    ((int8_t *)dst->data)[ch * N_cols + c] = (int8_t)roundf(v);
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                vibeasr_i8s_quant_i8(float_buf + ch * N_cols + col0,
+                                     (int8_t *)dst->data + ch * N_cols + col0,
+                                     col1 - col0, inv_scale, /*relu=*/0);
             }
         }
 
@@ -19186,13 +19071,11 @@ static void ggml_compute_forward_mul_mat_add(
 
             // Convert int32 -> float with combined_scale + bias, store in float_buf
             for (int64_t c = 0; c < n_cols; c++) {
-                int32_t * col_acc = acc_buf + c * ne01;
-                for (int64_t oc = 0; oc < ne01; oc++) {
-                    float val = (float)col_acc[oc] * combined_scale + bias_data[oc];
-                    float_buf[(col0 + c) * ne01 + oc] = val;
-                    float av = val < 0.0f ? -val : val;
-                    if (av > local_absmax) local_absmax = av;
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                const float amax = vibeasr_i8s_dequant_absmax(
+                    acc_buf + c * ne01, ne01, combined_scale, bias_data, 0.0f,
+                    float_buf + (col0 + c) * ne01);
+                if (amax > local_absmax) local_absmax = amax;
             }
 
         }
@@ -19219,13 +19102,10 @@ static void ggml_compute_forward_mul_mat_add(
         const float inv_scale = (global_max != 0.0f) ? 127.0f / global_max : 0.0f;
 
         if (col0 < col1) {
-            for (int64_t c = col0; c < col1; c++) {
-                for (int64_t oc = 0; oc < ne01; oc++) {
-                    float v = float_buf[c * ne01 + oc] * inv_scale;
-                    v = v < -127.0f ? -127.0f : (v > 127.0f ? 127.0f : v);
-                    ((int8_t *)dst->data)[c * ne01 + oc] = (int8_t)roundf(v);
-                }
-            }
+            // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+            vibeasr_i8s_quant_i8(float_buf + col0 * ne01,
+                                 (int8_t *)dst->data + col0 * ne01,
+                                 (col1 - col0) * ne01, inv_scale, /*relu=*/0);
         }
 
         // Thread 0 stores the per-tensor output scale
@@ -19296,12 +19176,11 @@ static void ggml_compute_forward_mul_mat_add_relu(
             }
 
             for (int64_t ch = 0; ch < C; ch++) {
-                for (int64_t c = 0; c < n_cols; c++) {
-                    float val = (float)acc_buf[ch * n_cols + c] * combined_scale + bias_data[ch];
-                    float_buf[ch * N_cols + (col0 + c)] = val;
-                    float av = val < 0.0f ? -val : val;
-                    if (av > local_absmax) local_absmax = av;
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                const float amax = vibeasr_i8s_dequant_absmax(
+                    acc_buf + ch * n_cols, n_cols, combined_scale, NULL, bias_data[ch],
+                    float_buf + ch * N_cols + col0);
+                if (amax > local_absmax) local_absmax = amax;
             }
         }
 
@@ -19324,12 +19203,10 @@ static void ggml_compute_forward_mul_mat_add_relu(
 
         if (col0 < col1) {
             for (int64_t ch = 0; ch < C; ch++) {
-                for (int64_t c = col0; c < col1; c++) {
-                    float v = float_buf[ch * N_cols + c] * inv_scale;
-                    v = v < -127.0f ? -127.0f : (v > 127.0f ? 127.0f : v);
-                    int8_t q = (int8_t)roundf(v);
-                    ((int8_t *)dst->data)[ch * N_cols + c] = q < 0 ? 0 : q;
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                vibeasr_i8s_quant_i8(float_buf + ch * N_cols + col0,
+                                     (int8_t *)dst->data + ch * N_cols + col0,
+                                     col1 - col0, inv_scale, /*relu=*/1);
             }
         }
 
@@ -19372,13 +19249,11 @@ static void ggml_compute_forward_mul_mat_add_relu(
             }
 
             for (int64_t c = 0; c < n_cols; c++) {
-                int32_t * col_acc = acc_buf + c * ne01;
-                for (int64_t oc = 0; oc < ne01; oc++) {
-                    float val = (float)col_acc[oc] * combined_scale + bias_data[oc];
-                    float_buf[(col0 + c) * ne01 + oc] = val;
-                    float av = val < 0.0f ? -val : val;
-                    if (av > local_absmax) local_absmax = av;
-                }
+                // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+                const float amax = vibeasr_i8s_dequant_absmax(
+                    acc_buf + c * ne01, ne01, combined_scale, bias_data, 0.0f,
+                    float_buf + (col0 + c) * ne01);
+                if (amax > local_absmax) local_absmax = amax;
             }
         }
 
@@ -19400,14 +19275,10 @@ static void ggml_compute_forward_mul_mat_add_relu(
         const float inv_scale = (global_max != 0.0f) ? 127.0f / global_max : 0.0f;
 
         if (col0 < col1) {
-            for (int64_t c = col0; c < col1; c++) {
-                for (int64_t oc = 0; oc < ne01; oc++) {
-                    float v = float_buf[c * ne01 + oc] * inv_scale;
-                    v = v < -127.0f ? -127.0f : (v > 127.0f ? 127.0f : v);
-                    int8_t q = (int8_t)roundf(v);
-                    ((int8_t *)dst->data)[c * ne01 + oc] = q < 0 ? 0 : q;
-                }
-            }
+            // vectorised epilogue (src/ggml-vae-i8_s-mad.cpp)
+            vibeasr_i8s_quant_i8(float_buf + col0 * ne01,
+                                 (int8_t *)dst->data + col0 * ne01,
+                                 (col1 - col0) * ne01, inv_scale, /*relu=*/1);
         }
 
         if (ith == 0) {
@@ -22114,6 +21985,61 @@ struct ggml_cplan ggml_graph_plan(
     return cplan;
 }
 
+// ---------------------------------------------------------------------------
+// Per-node graph profiler (VIBEASR_NODE_PROFILE=1).
+//
+// Every node is followed by a barrier, so thread 0 can time each one including the
+// wait for stragglers -- which is the number that matters, since that is what the
+// wall clock pays. Two views are accumulated: by section, delimited by tensors named
+// "MARK:<name>" that the caller plants at boundaries, and by ggml op, which is what
+// says whether time goes to arithmetic or to data movement.
+//
+// Off by default; the timing itself costs two clock reads per node.
+// ---------------------------------------------------------------------------
+#define VIBEASR_PROF_SLOTS 96
+struct vibeasr_prof_row { char key[64]; int64_t us; int64_t calls; };
+static struct vibeasr_prof_row vibeasr_prof_sec[VIBEASR_PROF_SLOTS];
+static struct vibeasr_prof_row vibeasr_prof_op[VIBEASR_PROF_SLOTS];
+static int vibeasr_prof_on = -1;
+static int64_t vibeasr_prof_pending = 0;
+static char vibeasr_prof_cur[64] = "(before first mark)";
+
+static void vibeasr_prof_add(struct vibeasr_prof_row * tab, const char * key, int64_t us) {
+    for (int i = 0; i < VIBEASR_PROF_SLOTS; i++) {
+        if (tab[i].key[0] == 0) {
+            snprintf(tab[i].key, sizeof(tab[i].key), "%s", key);
+            tab[i].us = us; tab[i].calls = 1; return;
+        }
+        if (strcmp(tab[i].key, key) == 0) { tab[i].us += us; tab[i].calls++; return; }
+    }
+}
+
+static void vibeasr_prof_dump_tab(const char * title, struct vibeasr_prof_row * tab) {
+    int64_t tot = 0;
+    for (int i = 0; i < VIBEASR_PROF_SLOTS && tab[i].key[0]; i++) tot += tab[i].us;
+    if (!tot) return;
+    fprintf(stderr, "\n%s  (thread-0 wall, includes barrier waits)\n", title);
+    fprintf(stderr, "  %-30s %10s %8s %10s\n", "key", "ms", "share", "nodes");
+    for (int pass = 0; pass < VIBEASR_PROF_SLOTS; pass++) {
+        int best = -1;
+        for (int i = 0; i < VIBEASR_PROF_SLOTS && tab[i].key[0]; i++) {
+            if (tab[i].calls < 0) continue;
+            if (best < 0 || tab[i].us > tab[best].us) best = i;
+        }
+        if (best < 0) break;
+        fprintf(stderr, "  %-30s %10.1f %7.1f%% %10lld\n", tab[best].key,
+                tab[best].us / 1000.0, 100.0 * tab[best].us / tot,
+                (long long) tab[best].calls);
+        tab[best].calls = -1;
+    }
+    fprintf(stderr, "  %-30s %10.1f\n", "TOTAL", tot / 1000.0);
+}
+
+static void vibeasr_prof_dump(void) {
+    vibeasr_prof_dump_tab("[vibeasr] time by section", vibeasr_prof_sec);
+    vibeasr_prof_dump_tab("[vibeasr] time by ggml op", vibeasr_prof_op);
+}
+
 static thread_ret_t ggml_graph_compute_thread(void * data) {
     struct ggml_compute_state * state = (struct ggml_compute_state *) data;
     struct ggml_threadpool    * tp    = state->threadpool;
@@ -22134,6 +22060,14 @@ static thread_ret_t ggml_graph_compute_thread(void * data) {
     for (int node_n = 0; node_n < cgraph->n_nodes && !tp->abort; node_n++) {
         struct ggml_tensor * node = cgraph->nodes[node_n];
 
+        if (vibeasr_prof_on < 0 && state->ith == 0) {
+            const char * e = getenv("VIBEASR_NODE_PROFILE");
+            vibeasr_prof_on = (e && *e && strcmp(e, "0")) ? 1 : 0;
+            if (vibeasr_prof_on) atexit(vibeasr_prof_dump);
+        }
+        const int64_t vibeasr_t0 =
+            (vibeasr_prof_on == 1 && state->ith == 0) ? ggml_time_us() : 0;
+
         ggml_compute_forward(&params, node);
 
         if (state->ith == 0 && cplan->abort_callback &&
@@ -22143,6 +22077,17 @@ static thread_ret_t ggml_graph_compute_thread(void * data) {
         }
 
         ggml_barrier(state->threadpool);
+
+        if (vibeasr_prof_on == 1 && state->ith == 0) {
+            const int64_t dt = ggml_time_us() - vibeasr_t0;
+            vibeasr_prof_add(vibeasr_prof_op, ggml_op_name(node->op), dt);
+            vibeasr_prof_pending += dt;
+            if (strncmp(node->name, "MARK:", 5) == 0) {
+                vibeasr_prof_add(vibeasr_prof_sec, node->name + 5, vibeasr_prof_pending);
+                vibeasr_prof_pending = 0;
+                snprintf(vibeasr_prof_cur, sizeof(vibeasr_prof_cur), "%s", node->name + 5);
+            }
+        }
     }
 
     return 0;
